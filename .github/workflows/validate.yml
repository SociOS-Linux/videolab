name: validate

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate videolab
        run: make validate
      - name: Offline smoke test
        run: make smoke
      - name: Emit SourceOS carry fixture
        run: make carry
